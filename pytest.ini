[pytest]
# Set a global timeout of 30 seconds per test to prevent hanging
timeout = 30

# Timeout can be overridden per test with @pytest.mark.timeout(seconds)

# Only test the tests directory, exclude examples
testpaths = tests